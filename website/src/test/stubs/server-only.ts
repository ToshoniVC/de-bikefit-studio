// Test stub for the `server-only` package (aliased in `vitest.config.ts`).
// The real package throws outside a React Server Components bundle; unit
// tests import server modules for their pure helpers only.
export {};
